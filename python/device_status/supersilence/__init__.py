"""SuperSilence — sonar console.

A remake. See ``AGENTS.md`` before changing anything, and ``legacy/`` for the
previous version, which is a reference and never an import target.
"""

#: The only place the version is defined. ``pyproject.toml`` reads it from here,
#: so bumping it in one place bumps it everywhere.
__version__ = "0.1.0"
